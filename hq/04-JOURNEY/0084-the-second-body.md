# Episode 0084 — The second body: the stack is general, measured (2026-08-10)

The owner named the risk after the hunger arc ("we need to make sure
we are not type-casting ourselves into only a game bot AI"), and the
honest accounting agreed on the exposure: every motivation-stack
measurement had run on one embodiment. The mechanisms were
body-agnostic *by construction* — channel indices as constructor
parameters, the head predicting whatever channels exist — but not
*by measurement*, and only measured closes a debate. The second-body
gate mounted the **identical shipped composition** on a deliberately
alien body and asked whether the behaviors reappear.

The sample-field body shares nothing structural with C1: absolute
compass moves instead of yaw-and-facing, 7 actions instead of 12, 16
channels instead of 34, position at different indices on a different
scale, collection on the rover's own cell, a two-step chain
(collect crystal → refine) instead of the crafting grid. It mounts
with no bridge at all — the engine's world surface directly, one
second per taught seed — which is itself the anatomy seam doing its
job. Zero changes to `src/pra`; the teaching protocol and the felt
channel mirror the arc exactly.

All three bars passed at 24-seed power [measured]:

- **Election transfers: 24/24** seeds complete the chain analog
  (floor without the composition: 2/24 — the arc's every floor,
  reproduced on a new body).
- **Taught order transfers: 24/24** seeds work a taught far
  station.
- **The emergency appetite transfers, maximally:** hungry-bin
  nourishing share 0.996 coupled vs 0.780 uncoupled, dial monotone
  0.870 → 0.967 → 0.996 — and the survival ladder is stark: drive
  alone dies **24/24** (frontier starvation, reproduced on body
  #2), the uncoupled composition dies **16/24** at the biting
  drain, the coupled composition dies **0/24**. On this body the
  coupling is not +4 lives; it is the difference between mostly
  dying and nobody dying.

Ops honesty: two open pre-arms amendments with pilot numbers (the
registered drain never bit on a body earning ~0.0066/tick — raised
0.0015 → 0.005; the S3 ratio form was ceiling-degenerate against
this body's naturally rich hungry diet of 0.742 — replaced by a
margin + survival form), one instrument bug (the teacher's felt
check stopped at the tape boundary; C1's tapes had accidentally
always ended on idle), and one recorded gap (the head's
prediction-error curve was not instrumented; its learning of the
alien body is evidenced behaviorally through the hold and the
itch [mechanism-argument]).

What it closes: the type-casting question — the stack's claims are
now embodiment-general as measured on two structurally disjoint
bodies, and the general vocabulary (acquisition channel, felt
channel, meter) is the documented language. What it opens: a
third body is cheap now (the port is constructor indices plus
teaching tapes); the head's learning curve should be logged
directly on any future port.

Reversal condition: none — records a completed measurement. A
future body port where the composition fails its election bar with
verified honest senses reopens generality with that body as the
isolating instrument.

Trail: topic second-body (folder retired; registration 55895e9,
pilots + amendments, verdict 99f047a); Doc 0010 generality section;
runner `sample_field.py`, the sf-graduates cohort, and row files in
the session scratchpad.
