# Bars A2/A3 — aimed contact and the ablation (2026-08-15)

**A2 as registered: FAILED (1/6 expressing, bar ≥ 4/6). A3's formula
"pass" is vacuous — what the ablation actually measured is that the
worth lookup is NOT the carrier: the decoupled arm read same-or-better
on every foraging measure.** Protocol: [`a23_arms.py`](a23_arms.py);
rows: [`a2-rows.json`](a2-rows.json), [`a3-rows.json`](a3-rows.json).
6 hungry-born 10,050-step lives per arm, worth form, taught brain +
taught tongue restored per life; A3 = `AIM_ABLATE=1` (book learns,
lookup naive, everything else byte-identical). Expressing = seen-forage
chain + satiation ≥ 17.

## The rows

| Arm | Life | Eats | First-eat | Chain | Satiated | Drops leg | Columns | Reach |
|---|---|---|---|---|---|---|---|---|
| A2 | 1 | 3 | 4,312 | – | – | – | 70 | 9.3 |
| A2 | 2 | 0 | — | – | – | – | 85 | 11.5 |
| A2 | 3 | **6** | **3,898** | ✓ | ✓ (19) | **✓** | 100 | 11.4 |
| A2 | 4 | 0 | — | – | – | – | 92 | 12.8 |
| A2 | 5 | 4 | 9,478 | ✓ | – | – | 108 | 12.4 |
| A2 | 6 | 0 | — | – | – | – | 93 | 10.9 |
| A3 | 1 | 3 | 5,038 | – | – | – | 119 | 12.3 |
| A3 | 2 | 4 | 1,771 | – | – | – | 129 | 11.4 |
| A3 | 3 | 7 | 2,962 | – | ✓ (20) | – | 106 | 11.5 |
| A3 | 4 | 0 | — | – | – | – | 109 | 12.7 |
| A3 | 5 | **8** | 6,423 | ✓ | ✓ (20) | – | 127 | 12.9 |
| A3 | 6 | 7 | 4,278 | – | ✓ (20) | – | 102 | 11.9 |

## What the arms measured

- **A2 FAILED as registered**: 1/6 expressing (bar ≥ 4/6); median
  first-eat among eaters 4,312 (bar ≤ 3,000). The one expressing life
  carried the registered **drops leg** — drop seen → approached →
  pocketed → eaten, read from the body's own channel — the first ever
  measured.
- **The ablation is the sharp result.** Same-or-better everywhere:
  eats-at-all 5/6 vs 3/6; total eats 29 vs 13; satiated lives 3 vs 1;
  strict expression 1 vs 1; median first-eat among eaters 4,278 vs
  4,312; mean columns 115 vs 91. The worth lookup at life-time is
  **not the carrier** of any foraging behavior measured here. The A3
  bar's formula returns "pass" (expressing ≤ 2), but that clause
  presupposed an A2 pass — recorded as vacuous, not claimed.
- **R-explore, the transformed baseline**: every life in BOTH arms
  roamed ~70–129 distinct columns with reach 9–13 blocks, where the
  flood-era stalled lives paced their taught lanes. Since both arms
  share it, the roaming belongs to the shared stack (sense-using
  teaching + flood + 10k horizon), not to the live worth lookup. The
  arms cannot separate teach-time aim contribution (both brains were
  TAUGHT with the aim channel present) — scoped honestly: it is the
  life-time lookup that measured null.
- Pooled at a 6,000-step horizon (the flood era's): 6/12 lives first-ate
  ≤ 6,000 (50%) vs the flooded band's ~38% — the modest pilot lift
  reproduced, attributable to the shared stack, not the aim.

## Interruption, recorded

The Minecraft server's client keepalive stalled during the first
attempt at A3 life 4 (~30 s timeout; bridge exited; `arms-run.log`
carries the traceback). The life was discarded whole and re-run from a
fresh hungry birth after the server answered rcon again — no partial
life contributes to the rows.

## Where this leaves the bars

A1 MET; A2 FAILED; A3 vacuous-pass measuring the decoupling null. The
registration's reversal path now applies: both forms have failed to
beat the floor (salience at pilot, worth at arms) — **the registered
positive control runs next**: the hand-wired decree (steer toward the
highest-priced seen appearance when hungry; skills stay the taught
brain's). Decree clears A2 → aiming is real but not learnable in the
record's grammar (reverse to design at the brain seam, fence question
to the owner). Decree fails → the gap is not aim (reversal fires on
the direction; motivation-stack ordering re-opens with these rows as
its floor).
