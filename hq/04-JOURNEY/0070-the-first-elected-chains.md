# Episode 0070 — The first elected chains: hold + itch, and the stack measured from the passing side (2026-08-08)

Written mid-topic, the 0055 precedent: a landmark finding does not
wait for graduation. The motivation-stack topic's first gate — G1, the
completion-pull — ran the same evening it was registered, and **both
bars passed**: the first time in the project's history that taught
knowledge converts into deliberate multi-step chains at pre-registered
power.

**The gate, in one line:** goal-homing's proven hold (λ = 0.25
clone-step potential) plus one new term — κ · (sensed dig-progress
after the candidate action minus now), completion counted as full —
the *completion itch*: start is worth a little, continuing is worth
more, abandoning costs everything sunk (the world's own mechanics:
any non-dig action resets the cracks) `[measured in source]`.

**The verdicts (24 seeds at κ\* = 0.25, H = 5,000):**

- **Bar A — ≥ 18/24 seeds gain a log: PASS 24/24**, 286 logs across
  the arm `[measured]`. The identical graduates under the identical
  hold, without the itch, dug **zero** logs in 24 runs (episode
  0069). One term separates a statue from a woodcutter.
- **Bar B — ≥ 6/24 seeds complete log→planks→sticks: PASS 6/24,
  exactly at the bar** (seeds 1, 3, 5, 11, 19, 20) `[measured]`.
  Reported as a pass at the line. The entire prior record held two
  chains, both existence-level.
- **Median dwell 100.0%** — the new term does not disturb the hold.
- **The decisive control — itch without hold (λ = 0, seeds 1–8): 2/8
  dig, 0/8 chain** `[measured]`. Wanderers itch only where they
  happen to stand. **The pass belongs to the composition.**

**What was refuted:** the author's Bar-A prediction under-shot for
the fourth consecutive gate (20–23 predicted, 24 measured); Bar B
landed inside its predicted range (4–9, measured 6) — the arc's first
calibrated prediction. More substantively: the reading, standing
since episode 0054, that no cheap seam-level mechanism converts
taught knowledge into reliable directed behavior — **two cheap
seam-level terms, composed, just did**, with zero src edits.

**What it opened:** per G1's frozen decision rule, layer 4 plus
position suffice for election, and the next registrations are the
**learnable itch** (the brain's own mining-progress channel replacing
the oracle — the sensed signal already exists in the observation) and
**E2.1 shaping as refinement, not rescue**. The stack hypothesis
(`hq/01-RESEARCH/motivation-stack/`) now has its first passing
measurement of the core claim: single layers fail, compositions
carry. The ugly-twin ledger stays honest: the itch is
target-agnostic — several seeds ground out hundreds of cobblestone at
κ ≥ 1 — and the perseveration watch is pre-named for the learnable
registration `[measured]`.

Reversal condition: the composition claim reopens if the learnable
itch (brain-side signal, no oracle) fails to reproduce Bar A at its
registered gate — the oracle would then be carrying more than the
mechanism `[judgment]` — or if a later gate shows chains at bar with
the itch absent, which would hand election to a cheaper layer and
simplify the stack.

Trail: `hq/01-RESEARCH/motivation-stack/README.md` (G1 registration,
pilot, outcome — this change-set); episodes
[0069](0069-goal-homing.md) (the hold), [0055](0055-the-first-deliberate-chains.md)
(the two existence chains), [0054](0054-two-doors-one-graduate-who-walks-away.md)
(the refuted no-cheap-mechanism reading); runner `itch_gate.py`
(session scratchpad, arc convention); rows
`itch-pilot-rows.json` / `itch-confirmatory-rows.json` alongside it.
