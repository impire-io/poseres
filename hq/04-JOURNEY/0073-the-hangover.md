# Episode 0073 — Expecting the well-done, perfectly; wanting it, backwards (2026-08-09)

G5 — approval, revisited — ran the same day it was registered, on the
owner's "open G5." It reopened the July gate that parked the self-set-goals
topic (episode [0057](0057-approval-without-expectation.md): approval
sensed 45 times, never reliably expected; refutation predictor-shaped, not
concept-shaped) with the machinery that shipped the day before (episode
[0072](0072-the-event-pathway-ships.md)). Both binding bars passed at
ceiling — and the context rows produced the arc's strangest finding yet.

**The reversal `[measured]`:** E3.0's instrument rebuilt from its
committed record (verdict channel 33, judge on stick-crafts, the 034 tape,
45 snapshot-bridged demonstrations, fresh 33-dim cohort), one addition:
`event_head_eta = 0.5` from the first step. Bars — July's own two,
statistics unchanged, predictor swapped:

- **P5-a rising: 24/24** (bar ≥ 18; July: 18/24). **P5-b specific:
  24/24** (bar ≥ 18; July: 14/24, five seeds anti-predictive).
- Every seed identical: last-5 completion-tick expectation **1.000**,
  off-tick **0.000** — perfect, specific, at ceiling, half-formed within
  five approvals. The head's cold-start-zero determinism ends the frames'
  expectation lottery: expecting stops depending on the luck of random
  initialization.
- The frames' context row, same run: **18/24 and 14/24 — E3.0's exact
  digits.** The instrument replicated July in the act of overturning it.
- Episode 0072's sparse-regime worry is answered for this pulse: 1-in-22
  sparse *in time* was not sparse *in structure* — the stick-offer's
  signature channels made the precondition linearly visible, and the head
  read it.

**The two context findings, both bigger than their status:**

1. **Teaching the predictor erased the cold-start barrier**
   `[measured]`. V0 — the shipped G3 composition, approval merely
   present — chains **24/24** on this cohort (G3's own cohort: 13/24).
   The one variable: this cohort's head learned *through the
   demonstrations* (its state riding feature 040's snapshot persistence
   between segments) and entered free-run already knowing the dig and
   craft dynamics. The parent's demonstrations now transmit
   *expectations*, not just frames — and expectation alone carried
   election to ceiling `[mechanism-argument]`.
2. **The post-approval hangover** `[measured]`: wiring the anticipated
   verdict into value — κ₅ · Δ̂[verdict] on the licensed seam — *reduced*
   approval earned at every dose tried: firings 572 vs 1,888 at
   κ₅ = 0.25 (chains 23/24 — capability intact, praise-earning taxed
   ~70%); at κ₅ ≥ 1, seeds collapse into log-hoarding with zero sticks —
   the bot stops doing the thing that earns praise entirely. Mechanism
   reading `[mechanism-argument]`: the verdict's 1 → 0 decay after each
   firing is learned online per action, so the praised loop's familiar
   continuations all come to predict approval-*loss* (−κ₅ every
   post-firing tick) while untried actions predict nothing — the term
   repels the agent from its own praised loop the moment praise lands.
   The pre-registered sycophancy watch closed inverted: **the measured
   failure mode of approval-as-value is avoidance, not sycophancy.**

**What was refuted or reversed:** July's "approval cannot be reliably
expected" — reversed at 24/24, exactly along its own scope note. The
author's frozen prediction missed high again (P5-b predicted 19–23,
measured 24 — the seventh consecutive under-prediction of a composed
mechanism) and missed the V+ call in the informative direction
("within ±2 chains" — the term instead cut earned approvals by 70%).

**What it opened:** per the frozen decision rule, **E3.1 — the
anticipated verdict wired into a want — reopens as the successor gate,
owner's design conversation first**, now with a measured hazard list its
registration must answer: the one-step reach wall (E2.0h, standing), the
hangover (this gate), and the avoidant collapse at dose. The stack map
gains an unforeseen ugly twin at layer 5 and a strengthened layer-5
transmission story: what the parent demonstrably transmits, via the
event pathway, is *what to expect* — and that alone moved election to
ceiling.

Reversal condition: the perfect-expectation reading is scoped to
pulses whose preconditions are linearly visible in the observation; a
future gate whose event lacks a sensed precondition (true structural
sparsity) failing to form expectations would split the claim without
reversing this one. The hangover reading reopens if a replication with
the decay excluded from learning (or the term gated off post-firing
ticks) fails to restore V0's earning rate — that would mean the tax
lives elsewhere `[judgment]`.

Trail: `hq/01-RESEARCH/motivation-stack/README.md` (G5 registration
3859b25, binding bars + pilot published d9f40bd, arm + closure — this
change-set); episodes [0057](0057-approval-without-expectation.md)
(July's gate), [0071](0071-wanting-follows-expecting.md),
[0072](0072-the-event-pathway-ships.md); runner `g5_runner.py`
(session scratchpad, arc convention), rows `g5-arms-rows.json` /
`g5-vplus-rows.json` / `g5-p5-stats.json` alongside it.
