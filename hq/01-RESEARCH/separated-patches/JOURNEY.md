# Journey — separated-patches (started 2026-08-23)

## 2026-08-23 — the rig and the declared distance ladder, before the walk

The 0111 rig restored from its trail (d940e7e) onto its own world
(`pgw2-minecraft`, port 25606, bridge 25594): `rig/sep.py` carries
the meter instruments unchanged, `rig/peer.js` is the 0109 hostile
policy byte-identical, and the world builder places **two patches**
— A at (5,5) beside the 0110 spawn stand, B due east at (5+D, 5) —
each holding C-1's total-war larder exactly (2 pre-grown melons, no
stems, no renewal; `random_tick_speed` untouched because nothing
regrows). The birth admin erases every site any rung can touch
(both probe patches, all three B positions) before building the
declared rung.

**The one walked variable** is the separation D. Declared rungs:

| rung | D (blocks) | note |
|------|-----------|------|
| D12  | 12        | patch B at the glance sense's 16-block edge from A's melons |
| D24  | 24        | beyond any single glance; ~0109's inter-patch distance |
| D48  | 48        | far: multiple glance-horizons of travel |

**Walk order, declared:** outward from D12, mechanized in
`rig/walk.py` (the 0111 rule verbatim): solo-86 first (Bar S0 —
zero starvation, ≥ 3 eats, below-12 ≤ 0.10; a rung failing solo is
disqualified and gets no hostile arm), then hostile-86 (the Bar S1
hunt). Stop at the first hostile reading in 0.10–0.90; that rung
freezes (`FROZEN.json`, a hand act journaled here) and Bar S2's
replicate runs on it. If adjacent rungs straddle the band,
intermediate distances may be added as declared rungs before they
run (the standing rule from 0110/0111).

**Registered expectation (mechanism note, not a bar):** the peer
shadows the subject, so at any instant one patch is camped and one
is free; the subject's cost of using the free patch is travel plus
finding it (at D48 patch B sits beyond the glance horizon — S0
exists precisely to read whether the blind body can use a larder it
cannot see from the other). If the middle exists on this axis it
should appear where travel time is comparable to the peer's
reacquisition time — the D24 neighborhood — with D12 expected near
total war (both patches inside one contested neighborhood) and D48
gated on S0.

**Confounds accepted going in:** one life per arm (S2 exists for
the rung that matters); the same taught 86 draw in every arm; the
peer's pathing at distance is the 0109 straight-line walker with
its stuck-escape ladder — its act log tells whether it commuted or
camped.
## 2026-08-23 — the walk completes: 1.000 at every distance; the war is at the birth minute, not between the patches

The mechanized walk, one life per arm [measured]:

| rung | solo below-12 ss (eats) | S0 | hostile below-12 ss (eats) | peer digs |
|------|-------------------------|-----|----------------------------|-----------|
| D12  | 0.000 (6) | PASS | 1.000 (0) | 4 |
| D24  | 0.000 (7) | PASS | 1.000 (0) | 4 |
| D48  | 0.000 (3) | PASS | 1.000 (0) | **2** |

Every rung passes its solo screen — at D48 the blind body found and
used a larder two glance-horizons away, at exactly the 3-eat
minimum. Every hostile arm reads 1.000. The act logs behind the
claim (the standing guard): at D12 and D24 the peer's four digs
consumed the entire split larder; at D48 the peer dug **only patch
A's two melons — patch B's larder stood untouched for the whole
life — and the subject still ate nothing** (0 eats, 0 collects,
food to 2, health untouched: hunger's edge, 0110's signature).

Distance does not open the middle, and the act logs say why
[measured + mechanism-argument]: the war is decided in the birth
minute at the near patch, exactly as in 0110 and 0111 — and a
hungry-born subject that lost its opening never expresses the
far-patch journey, even when nothing contests it. The D48 solo's
3-eat knife edge shows the journey is marginal even undisturbed;
any birth-minute disruption kills it. The middle was never between
the patches because the contest never leaves the first one.

**The registered reversal fires:** no rung in the band after S0
screening. Axis 2 closes on both its arms — rate (0111) and
separation (here) — the third null, and design 0017's last
standing candidate is comparable-speed bodies. Routing per the
pre-registration: graduate abandoned.
