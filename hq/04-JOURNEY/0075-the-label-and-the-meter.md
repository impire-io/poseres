# Episode 0075 — The label that couldn't reach and the floor that outran learning (2026-08-09)

The other two tracks of the owner's parallel-and-autonomous afternoon:
E3.1 (praise as a label — the design from the owner's own
label-not-fuel conversation) and G4 (the meter — the Fry argument made
mechanical). Both registered with frozen bars before any run; both
decided the same evening; neither passed clean, and both failures name
their successor precisely.

**E3.1 — T1 FAIL 0/24, T2 PASS 22/24** `[measured]`. The parent
applauded cobblestone — a preference the bots demonstrably lacked — and
praise entered only as a completion label (fired completions count
`1 + β·clip(Δ̂[verdict],0,1)`; the level never valued, so the hangover
cannot form). The label was **perfectly safe and perfectly inert**: not
one cobblestone at any dose (β ∈ {0.5, 1, 2}, pilot and 24-seed arm),
while chains held at 22/24 and 1,492 sticks — no trace of the κ₅ term's
−70% damage. The mechanism is the myopia the decision rule pre-named
`[mechanism-argument]`: the label pays one step from a finished mineral
dig, and nothing values the walk to the mineral face; the wood loop is
an attractor the bot never leaves (dwell 100%). **Per the frozen rule,
E2.1 — rollouts / means-ends / skills — upgrades from refinement to
layer-5 prerequisite**: expectation is perfect (G5), the label is safe
(here), and neither steers without multi-step reach.

**G4 — M1 PASS, M2 FAIL-informative** `[measured]`. The meter: energy
decays 0.0005/tick, +0.1 per pocket-gain tick, death at zero.
Frontier-alone starves on the idle clock — median survival **2,001**
ticks, dwell < 1%, zero gains: E0's null now has a body count. The
composition arm split the bar: **24/24 seeds working** (all gained
logs; 1,905 sticks; the miser twin silent, unique positions median 22)
but only **10/24 alive** at 5,000 — the cold-start runway (~2,000
ticks) is shorter than the cohort's median time-to-first-chain
(~2,300). The layers do not conflict; **the floor races learning and
wins** `[mechanism-argument]`. What covers exactly that gap in every
real creature is provisioning — the parent feeds the child until
competence pays — which couples layer 1 to layer 5 and makes the
meter's honest dose a design decision (a provisioning phase), not a
quiet re-dose `[judgment]`.

**The prediction ledger turned over twice:** T1 predicted 14–20,
measured 0 — the first *over*-prediction after seven under-predictions;
M2 predicted 22–24 alive, measured 10. The pattern sharpens into a
lesson: this author systematically *under*-predicts what composed
mechanisms do and *over*-predicts what one-step terms can reach.

**Where the stack stands after the afternoon:** expectation ✓ (G5),
completion ✓ (G1/G3), position ✓ (0074 — brain-side), floor ✓-with-a-
timing-coupling (G4), transmission ✗ blocked on reach (E3.1). One
prerequisite now sits under everything still open: **multi-step
reach** — E2.1 is the arc's next load-bearing question, and it is a
design conversation, not an evening gate.

Reversal condition: E3.1's null reopens if a reach mechanism later
elects the applauded context and the label still fails to steer there
(the label itself, not reach, would then be refuted); G4's split
reopens if a provisioning-dosed meter still starves working seeds (the
coupling would be deeper than timing) `[judgment]`.

Trail: `hq/01-RESEARCH/motivation-stack/README.md` (registrations
75b2708, outcomes 7434732); episodes
[0073](0073-the-hangover.md), [0074](0074-brain-side-hold.md); runner
`parallel_gates.py`, rows `e31-phase1-rows.json` / `e31-plus-rows.json`
/ `g4-rows.json` (session scratchpad, arc convention).
