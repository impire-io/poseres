# the-last-crack — journey (opened 2026-08-15)

## 2026-08-15 — L1: the knife-edge convicted; the fix built; the twin measured (L1-READING.md)

The trace acquitted the clip (0/4 directed releases in its band,
releases scattered at progress 0.011–0.546) and convicted something
sharper: the vote is a knife-edge — directed release margins 0.00002
to 0.069 against a hold margin of only κ·Δ̂ ≈ 0.008 — and the ε-gate
kills WINNING holds (all three explore releases had DIG ahead on the
value table, one at progress 0.994, one frame from the break). A
30-frame live dig must win ~30 consecutive near-coin-flips with a
1-in-11 random break on top. Both mechanisms convicted → both fixes
landed opt-in in CompletionItchPolicy (constitution I pattern —
defaults bit-exact, full suite green): `commit_kappa` incumbency
(0.1, above the largest measured flip margin) and
`explore_defers_holds`. The first committed L2 arm then measured
commitment's degenerate twin on schedule: a 517-frame DIG lock
across breaks — perseveration, the addiction machine — closed by the
intention boundary (a progress collapse clears the incumbent;
incumbency dies with its intention; unit-tested). Second committed
arm read 7-vs-1 breaks on the same config across runs — live-world
variance too high for single 1,500-step arms — so the L2 pair is
powered to 3 repeats per arm with totals compared, the amendment
recorded here with the raw numbers that forced it.

## 2026-08-15 — pre-run amendment: ε joins the suspect list

Before any experiment ran, reading the policy for the trace design
surfaced the second candidate: `exploration_epsilon = 0.1` breaks a
held frame with p ≈ 0.092 (random ∧ non-dig), giving a 30-frame hold
only ~5% survival — enough on its own to explain near-zero
completions, sharpened by the LIVE world's rule that block damage
resets on release (the lab world never enforced it; the taught
ecology grew up interruption-tolerant). L1 amended openly to an
attribution bar: every release classified directed (value crossover,
the clip's band) vs explore (the ε-gate), and the fix that runs is
the one the measurement convicts. Both candidate fixes named in L2;
if both convict, both land and the pair runs the composition.
