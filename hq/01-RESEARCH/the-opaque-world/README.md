# The opaque world — does composition pay where position must be carried, not sensed?

**State:** active
**Started:** 2026-08-25

## Abstract

Episode 0118 measured the compositional gate on the frozen echo-teacher
world and the tower won on parsimony — but that world hands sequence
position to the senses through its progress channel, which is the
special case, not the general one (the owner's observation,
2026-08-25). Real worlds are position-opaque: Minecraft's forage chain
(see the melon, walk, dig, collect, hold, eat) has no channel anywhere
saying "you are at step 4 of the plan." 0118's own reversal condition
names exactly this arena. This topic runs the relocated comparison on
live Minecraft — the survival rig's probe world: a real server, real
mineflayer body, real food economy, provisioned melon patches for
measurability (the house's established live-measurement standard;
nothing faked) — with wild-world C1 adoption the named follow-on if
the mechanism earns it. It also measures the snapshot-topology
lifecycle cost 0117 named: references must survive the teach→life
serialization boundary or the primitive's price is on the record.

## The question

On live Minecraft, where plan position must be carried rather than
sensed, does a composition tier improve the taught forage life — and
does the identity-bound reference form (0118's loser on the
transparent world) beat the stacked tower there, as 0118's reversal
condition predicts it could?

## Pre-registered bars

Protocol common to every arm: the survival rig's probe world and
machinery exactly (`examples/minecraft/survival/` — the blessed stack,
design 0015's body at obs 86 / 13 actions, the d23 curriculum: 45
interleaved V0/V1/V2 lessons, hungry-born lives of 6,000 steps — the
d23-calibrated length, 2× the measured expression latency — at the
rig's 5× fabric). Arms: **flat** (the rig unmodified), **tower**
(same-step slot composition), **bind-pred** (identity-bound carried
prediction) — the 0118 scaffolding at the transition seam, the frame
mechanism the ONLY variable; same tapes, same hunger dosing, same
seeds. **Order: flat first** — its teach and 8 lives land on the
record before any composed arm runs (the T0 discipline). Then tower,
then bind-pred, 8 lives each.

- **Bar M0 — the scaffolding is live-real and snapshot-honest**
  (instrument; gates everything): (a) tier-2 alive and mapping ≥ 0.95
  of steps through teach and lives at C1 scale; (b) the cross-tier
  arbiter genuinely engages in the wild — tier-2 holds the policy
  predictor on ≥ 5% of directed steps in at least one life segment;
  (c) 0117's snapshot-topology cost measured payable:
  encode→decode→encode of the composed state is byte-identical, and a
  life resumed from snapshot continues tier-2 intact (population and
  reference ids, no reset). FAIL on (c) = the lifecycle cost is
  unpayable as prototyped; the topic routes with those numbers.
- **Bar M1 — composition transfers to the wild:** at least one
  composed arm beats flat on total eats across its 8 lives, with
  first-eat latency not worse beyond life spread. FAIL = 0118's
  capacity win is lab-bound at this dose — a design finding for 0020,
  recorded with the numbers.
- **Bar M2 — the relocated R0 (the headline):** bind-pred vs tower,
  eats across 8 lives, beyond life spread, on the world that hides
  position. PASS = references earn their keep exactly where 0118's
  reversal condition predicted, and the gate's shape re-opens with
  live numbers. FAIL = the tower's parsimony verdict extends to the
  opaque world and the reference primitive is refuted at this dose in
  the wild too.

## Reversal condition

If the flat arm fails to reproduce d23-class foraging at all (zero
eats across its 8 lives), the rig is re-validated before any composed
reading — an instrument failure, not a verdict. If the protocol
proves too noisy to discriminate (every pairwise arm difference on
every meter inside one life-spread SE at n = 8), the protocol scales
or re-scopes openly rather than declaring a null. And the standing
condition this topic exists to test: a reference win here reverses
0118's shape verdict for position-opaque worlds; a reference loss
here extends it, and the tower's parsimony claim graduates from
lab-bound to measured-in-the-wild.

## Verdict

<Empty until graduation. Filled by /research-graduate: PASS/FAIL per
bar with the honest numbers, each load-bearing claim tagged
[measured] / [mechanism-argument] / [judgment].>
