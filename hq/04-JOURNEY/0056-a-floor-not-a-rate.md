# Episode 0056 — A floor, not a rate: the horizon buys nothing (2026-07-24)

E2.0h, the horizon read — the owner's pick from episode 0055's fork,
option (ii) — asked whether E2.0's two-chains-in-42-runs was a rate that
climbs with the window: the same 24 E1 graduates under `GoalBiasPolicy`
at λ = 4, run to **H = 40,000** (8× the E2.0 window), incidence C(H)
read at nested points, F frontier control and a λ = 0.25 context arm
beside it. Bars frozen before any run: PASS iff C(40k) ≥ 6/24 (E1's own
k) with growth beyond C(5k); FAIL iff ≤ 2; memoryless reference ≈ 6.9
expected if chaining-on-touch were constant-rate.

**The instrument had to be rebuilt first, and the record was enough.**
The E1/E2.0 runners were scratchpad-only per the arc convention and did
not survive their session, so a P0 rebuild gate was registered: the
reconstruction — teacher tape, 45 snapshot-bridged segments per seed,
goal observation, policy mirror, detector — must reproduce E2.0's
recorded confirmatory before any horizon step is read. It did, exactly
[measured]: chain set {seed 7} at tick 427 (deterministic, as recorded),
0/24 at 20% dwell, median dwell 3.76% against the recorded 3.8%, first
operationalization, no variants. The registered-record discipline paid
its way: a lost instrument was rebuilt bit-faithfully from prose.

**The read: registered FAIL, and flat everywhere [measured].**
C(5,000) = C(10,000) = C(20,000) = C(40,000) = **1/24** — seed 7's
tick-427 chain and nothing ever after. Across all 24 G-λ4 seeds: 6
log-ups, one conversion, no seed chains twice. The λ0.25 arm replicates
E2.0's other recorded chain exactly (seed 6, tick 706) and adds
nothing. F control: 0 chains in 8 × 40,018 steps. Dwell after the first
5,000-step block is 0.0 in every arm and every seed (two seeds carry a
trace into block 1, none further); per-block mean Chebyshev distance
wanders to 100–1,300. The pre-named no-gradient failure mode that broke
E2.0's holding is hereby measured to break its rate too: **chains are
decided in the departure window** — both chains in the project's record
land inside the first ~700 ticks — and once the runner leaves the λ
term's capture radius it never re-enters. Patience buys nothing; the
chain probability is per-departure, not per-window [measured for the
where/when; mechanism-argument for the why].

**Refuted:** the frozen prediction (C(40k) ∈ [4, 10], PASS marginally
more likely) — the memoryless model was the wrong model. That is two
registered gates in a row deciding against their author's frozen
expectation; the bars keep doing their job, and the calibration lesson
is recorded with them. **Consequence, per the registered decision
rule:** the λ-on-one-step-lookahead form is exhausted for holding
(E2.0) *and* rate (E2.0h). Orbit-and-touch is not a mechanism; it is
one roll of the dice at graduation.

**What it opened:** the fork narrows to two live options plus the
default, still the owner's — (i) **E2.0b**, a homing-capable goal term
(potential-shaped distance, goal-conditioned frames), now with a sharper
target: the mechanism must create gradient *outside* the departure
window; (iii) **E3**, the teacher-model reread — the verdict channel as
what holds attention rather than distance; or **park**, with the
existence proof standing (knowledge + weak wanting produced the record's
only deliberate chains) and the c1c run as the watch.

Reversal condition: whichever fork option is picked must pre-register
its gate and bars before any run; if none is picked, the topic parks
with the existence proof as its standing result and the c1c run (E0's
null arm) as its watch — and the topic-level reversal stands: if c1c
ever chains on its own, the premise weakens and the topic pauses.

Trail: `hq/01-RESEARCH/self-set-goals/README.md` (E2.0h registration
1023033, P0 + pilot published pre-confirmatory 255b530, outcome
ad55243); runner scratchpad-only (`e2h_runner.py` beside the E1/E2.0
lineage); throughput probe calibration-only, declared in the
registration.
