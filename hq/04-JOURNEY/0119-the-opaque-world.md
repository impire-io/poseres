# Episode 0119 — The opaque world: composition indistinguishable in the wild, the lab's winner nominally last, the lifecycle benign (2026-08-25)

The owner's correction to 0118's scope, run to its locked verdict
the same day: the echo world hands sequence position to the senses —
the special case — so the composition comparison was relocated to
live Minecraft, where the forage chain (see, walk, dig, collect,
hold, eat) carries no plan-position channel anywhere. The survival
rig's probe world and machinery ran it exactly (real server, real
mineflayer body, real food economy — nothing faked): flat vs tower
vs bind-pred, same 45-lesson taught curriculum, same hungry-born
6,000-step lives, deficit gate OFF per the blessed stack, the frame
mechanism the only variable, lives interleaved round-robin against
world drift (amendment 1, pre-run, d3's own precedent).

**M0 PASS in full [measured]:** the 0118 transition-seam scaffolding
is live-real — tier-2 map rate ≥ 0.9986 through 135 lessons and 32
composed lives; the cross-tier arbiter held the policy predictor
31–52% of calls in every composed life (the clause asked 5% once).
The composed state ships as a pair — the untouched kernel blob plus
a tier-2 sidecar in the same no-pickle discipline — and resumed
byte-exactly on the cheap world, round-tripping byte-identically at
all 90 composed lesson checkpoints; the kernel-native single-blob
topology is the named design item. And 0117's eviction fear is
measured benign: every bind-pred life churned ~74 orphan events
with ~26 of ~39 references dangling at end, and the population
never destabilized — graceful degradation holds under real churn.

**M1/M2 as locked before the data [measured]:** at n = 8 every
pairwise difference sat inside one SE, the registered noise clause
fired, and the protocol scaled to n = 16 with the verdict rule
declared first. At n = 16: flat 41 eats (9/16 lives eating), tower
25 (4/16), bind-pred 38 (8/16); paired by round, bind − tower
+0.81 ± 0.91 SE, bind − flat −0.19 ± 0.64, tower − flat
−1.00 ± 0.78 — every difference inside spread. **Composition is
measured-indistinguishable from flat at this protocol scale on the
real position-opaque world**; neither transfer nor the
reference-over-tower separation is demonstrated, and neither is
refuted beyond spread. Recorded alongside, sub-spread and not
leaned on: the transparent world's parsimony winner is nominally
LAST on every behavioral meter, and bind-pred doubles flat's
seen-forage chains (6 vs 3) with the most distant collects (9). The
lab ordering did not transfer; if anything it inverted, inside
noise.

What it taught [judgment, mechanism-argument]: wild behavior is
carried by the recipe/itch stack and the event head — the frames
speak only through drive valuation of one-step predictions, so the
composed tiers changed WHO holds the predictor without changing
what the life does. And the probe world's forage chain, though
position-opaque, is short-horizon (~60–100 steps per chain) under
heavy policy scaffolding: the deep-sequence demand that motivated
the compositional gate never binds these meters. The honest state
of the gate after 0118 + 0119: no tested shape — flat, stacked, or
referencing — has demonstrated a composition win anywhere except
the tower's lab-interior shift, which did not transfer; the
missing arena is a position-opaque LONG-HORIZON world.

Reversal condition: a beyond-spread composition win (either shape)
on a registered long-horizon position-opaque protocol — a task
whose chains exceed the policy scaffolding's reach — reopens the
gate's shape question with those numbers and that world; a
beyond-spread tower-behind-flat separation on a scaled repeat of
THIS protocol would turn the nominal inversion into a measured
cost of imposed hierarchy in the wild and caution the gate's
default shape directly.

Trail: topic the-opaque-world (rig committed before removal:
opaque_runner.py, compose.py with the sidecar, snap_test.py, the
lives and telemetry rows); design
[0020](../02-DESIGN/0020-sequence-encoding.md) (updated with the
wild reading); episodes [0117](0117-compositional-frames.md),
[0118](0118-compositional-frames.md),
[0103](0103-native-survival.md) (the rig's provenance); commits
a97a82a (registration), a421b55 (M0(c) + amendments), 9a725ed
(rounds 1–8, scale clause), 4171779 (the locked verdict + rig).
