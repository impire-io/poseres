# Separated patches — does distance open the middle a camper closes?

**State:** active
**Started:** 2026-08-23

## Abstract

Partial-gap-world (episode 0111) measured that supply rate cannot
open a middle against a camper: on one patch, the hostile peer
stands where renewal lands and takes every unit at script speed —
total war at every rate. Its reversal names this topic: spatial
separation, design 0017's last axis-2 arm. Two larders far apart
mean the shadowing peer can only camp where the subject is; WHERE
the rival stands decides WHAT is contested, and travel time is the
resource no camper can confiscate. The hunt is unchanged: a
declared config whose hostile-86 below-12 steady-state lands in
0.10–0.90 — the partial-gap world that reopens the peers sense's
outcome license (design 0017, definition of done item 1). No rung
in the band closes axis 2 entirely, leaving comparable-speed bodies
(axis 3) as the design's last candidate.

## The question

With the larder split across two spatially separated patches, does
a declared separation distance produce a world whose hostile-86
below-12 steady-state lands in 0.10–0.90, where one patch produced
only 0.000 or 1.000?

## Pre-registered bars

Common protocol, inherited from 0110/0111 unchanged: live Minecraft
only, tick rate 100 / 50 ms, engine seed 1, blessed 86/13 default
body, hungry-born free roam, one life = 15,075 steps, meter window
segments 2–3, "below-12" = fraction of window steps with food
< 12/20, the 0109 hostile peer unchanged, views persisted, every
peer act logged, one life per arm with the walk on the record. The
world: **two patches, 2 + 2 non-renewing melons** (C-1's total-war
count at each site; no stems, no renewal — distance is this topic's
ONE variable), separation ladder declared in JOURNEY.md before the
walk. On its own world (`pgw2-minecraft`), fresh rig restored from
the 0111 trail.

- **Bar S0 — the split world stands solo:** each rung's solo-86
  life shows zero starvation health-loss, ≥ 3 detected eats, and
  steady-state below-12 ≤ 0.10. A rung failing solo is disqualified
  (at large separations the far patch may simply be beyond the
  body's senses — that is a finding about the body, not a war).
- **Bar S1 — the partial gap exists (headline):** some declared
  rung, frozen before its hostile arm runs, yields hostile-86
  below-12 steady-state in **0.10–0.90**. PASS freezes that config
  as the partial-gap world — design 0017's gate, found.
- **Bar S2 — the gap replicates:** a second hostile-86 life on the
  frozen config lands in the band again (0111's one-life economy,
  answered the same way 0110's was).

## Reversal condition

- **No declared rung lands in the band** after S0 screening: axis 2
  is closed on both its arms (rate, episode 0111; separation, here)
  — the all-or-nothing verdict holds wherever a script-speed
  shadower meets a taught brain, and design 0017's remaining
  candidate is comparable-speed bodies alone. Graduate abandoned
  carrying the third null.
- **S2 fails after S1 passes:** the band was between-life variance;
  no config freezes; any continuation registers a multi-life
  protocol first.
- Standing guard (0109/0110/0111): no claim about what the bodies
  are doing enters JOURNEY.md without a channel reading or an act
  log behind it.

## Verdict

<Empty until graduation. Filled by /research-graduate: PASS/FAIL per
bar with the honest numbers, each load-bearing claim tagged
[measured] / [mechanism-argument] / [judgment].>
