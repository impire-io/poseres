# Episode 0107 — The motivation stack: the map that ran the arc (2026-08-08 → 2026-08-16)

The topic that ordered everything since the goals ladder reopened:
motivation as LAYERS — filter, budget, deficits, option-value,
completion, borrowed goals, imagination, identity — each patching the
degenerate twin of the layer below and bringing its own, with every
null in the record re-read as an upper layer tested over a missing
floor. Eight days later the map has run fourteen gates and four
spawned topics, and its founding question — election — is answered in
the record's own terms: the blessed stack elects, sustains, and
survives on a real world's economy (episode 0103).

Per-layer close-out [measured unless tagged]: the **filter** is the
world's (native-survival's reframe). The **budget** measured true
(G4: stakes work; G4b: the taper to the tick) and then the synthetic
meter retired for the world's own. **Deficits** produced the arc's
biggest inversion: knowledge alone null (E1), the shipped gate (042)
inverted live and parked off — while the layer's REAL form measured
as the flood: sensation, not valuation; intrusion, not gain.
**Option-value**'s G2 never ran [judgment: the palate's worth channel
covers the sensing half of "a log is money"]. **Completion** is the
arc's crown: the itch (G1) begins work; COMMITMENT (0101, shipped
043) finishes it — and its twin, perseveration, arrived exactly as
the stack predicts, closed by the intention boundary. **Borrowed
goals** hold G3's event pathway (shipped 040), G5's perfect
expectation with the hangover as the twin on record, and recipes +
labels as the working transmission channel. **Imagination** stays
one-step; **identity** stays the map's top.

Graduated **to design**: [0016](../02-DESIGN/0016-motivation-stack.md)
carries the measured map — what each layer is, its twin, its
evidence, its shipped form — so future gates argue against a document
instead of a memory. Named open rungs: G2 option-value, deeper
imagination, and the gate-inversion mechanism.

Reversal condition: if a capability gate passes by adding machinery
that belongs to NO layer of the map (observable: a measured mechanism
whose removal breaks the gate and which the design cannot place), the
stack's decomposition is wrong, not just incomplete — the map itself
reopens.

Trail: topic motivation-stack (G1/G1L pre-registrations and outcomes
in git history after removal); episodes 0069–0073 (the ladder),
0074/0075, 0083–0085 (the gate), 0089 (the tongue), 0096 (c1e),
0100–0104 (the closing arc); design
[0016](../02-DESIGN/0016-motivation-stack.md).
