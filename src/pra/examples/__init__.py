"""In-repo example worlds (ROADMAP Phase B) — the getting-started experiences.

Examples mount through the same public seams a user's own world would
(``Engine(config, world_factory=...)`` + the Doc 02 anatomy layer); nothing
in here is imported by the core, the engine, or the validation harness.
"""
