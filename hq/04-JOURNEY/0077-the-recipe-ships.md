# Episode 0077 — The recipe ships, and the closure is exact again (2026-08-09, night)

Feature 041 (v1.3.0), the owner's "go on all fronts": the recipe-reach
and E3.1 mechanisms promoted to product the same day they were measured
— the praise label as keyword-only additions to the shipped itch (read
only inside fired completions, so the G5 hangover mechanically cannot
form; off = bit-exact v1.2.0 `[measured]`), and `pra.action.recipe`:
`RecipeMemory` (terminals = applauded endings) + `RecipePolicy` (the
argmax-valued recipe's stepping stones held via the event head's
predicted positions, the itch working each station). All anatomy
knowledge stays constructor-side; the surface grew additively (Doc 0008
both directions); 27 new unit tests; full gate green.

**The closure `[measured]`:** the recipe-reach 24-seed arm rerun on
shipped classes reproduced the gate **row for row** — 24/24 rows
identical (R1 24/24, R2 18/24 at bar, 3,129 cobble events) — the
second consecutive feature to close at behavioral identity (feature
040's standard). The topic graduates to
[Doc 0010](../02-DESIGN/0010-recipes-and-the-label.md); its named
successor (recombination — true means-ends over recipe fragments)
awaits its own topic.

Reversal condition: none — records a completed build/measurement; the
topic's standing reversal conditions carry into Doc 0010.

Trail: `specs/041-recipe-label/` (spec 31290f9, impl 72a26a6); topic
`hq/01-RESEARCH/recipe-reach/` (retired this commit; verdict + closure
in git history); closure runner `recipe_closure.py` +
`recipe-ship-rows.json` (session scratchpad); episodes
[0076](0076-the-steps-not-just-the-ingredients.md),
[0072](0072-the-event-pathway-ships.md).
