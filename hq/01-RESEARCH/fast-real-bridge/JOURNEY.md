# fast-real-bridge — investigation journal (started 2026-08-09)

**2026-08-09 night — topic opened, bars frozen before any run** (rung 2
of the owner's ladder). Key discoveries at registration: vanilla ships
/tick rate (no fork); tick_ms is already the pacing parameter
(250/M = the c1c posture); the only bridge change is reporting
bot.time.age. The wall-clock-physics distortion (bot slows 1/M relative
to world dynamics) is registered openly as part of what B2/B3 bound.

**2026-08-09 late night — the gate splits: the time machine is real,
the hands were never reliable.** B1 PASS at every M (exact TPS to 200,
ceiling unreached), B3 PASS (pacing law 5.02–5.29), B2 unmeasurable as
written — the 1× reference itself fails 5/5 (digs 3,4,2,4 of 5, no
speed trend; mineflayer aborts mid-break, a bridge-native behavior c1c
never needed to measure). Four instrument bugs found and fixed en
route (EPIPE crash, rcon flag-parsing, the 180° yaw convention split,
wall-clock walk displacement — the registered distortion measured for
the first time). Successors named: the bridge dig-robustness fix, then
B2′ as a relative bar at n ≥ 20.
