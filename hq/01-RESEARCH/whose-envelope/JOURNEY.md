# Journey — whose-envelope (started 2026-08-24)

## 2026-08-24 — the stack mapping, journaled before any arm

The rig is 0115's `seq.py` restored frozen (a882ef4). The stack arm
adds one policy mode: `CompletionItchPolicy` at the shipped
operating points (κ = 0.25, commit_kappa = 0.1,
explore_defers_holds on, completion_threshold 1/128, label and
deficit off) with the event head at η = 0.5 — the composed stack of
designs 0014/0016 as one declared variable against the plain drive.
The anatomy mapping: **progress_index = the world's progress
channel** (obs −2); **pocket_index = the pulse channel** (obs −1) —
a predicted pulse gain IS the acquisition analog, so a completion
fires exactly on a predicted acceptance (+1 from 0/−1 clears the
threshold by construction). The recording wrapper rides unchanged;
stack rows add the honesty counters (completions_fired,
false_completions, progress_pred_error_ema).

## 2026-08-24 — the shipped dose: conformity moves everywhere, acceptance pays at the switch; E0 FAIL routes the dose ladder

Stack vs the inherited plain baselines, 24 seeds [measured]:
violations/step improve at EVERY rung and the improvement GROWS
with difficulty (−0.015 / −0.067 / −0.063 / −0.131 / −0.187 at
R2/C2/R4/R8/C4 — C4 lands at 0.353, far below the plain plateau)
— the stack gives the drive a reason to stay on pattern, and it
does. But acceptance: R2 16.0 (passes its 9.23 line), C2 **6.769
vs 24.615 plain** — slashed 3.6×, failing its line — R4 0.0. **Bar
E0 FAIL at the shipped dose**, per its registered path: the dose
may be amended openly before any E1 claim. The honesty counters
carry the suspect [measured]: false completions 35–52% (peaking at
C4's 2,796 median), and the C-family is where sequences SWITCH
tokens at the boundary — commitment's incumbency fighting the
switch is the named mechanism [mechanism-argument].

**The dose ladder, declared before it runs:** two amended
configurations, one component each — (a) κ = 0.05, κ_c = 0.1 (the
praise-cliff lesson: small doses nudge); (b) κ = 0.25, κ_c = 0
(commitment ablated: does incumbency, not the itch, cost the
switch?). Both on the E0 rungs only (R2/W2, C2/DK); an
E0-passing configuration, if any, carries E1/E2; if none passes,
the registered no-dose-passes routing (a design 0016 boundary
finding) takes the verdict.

## 2026-08-24 — the dose ladder: commitment was the tax, the itch alone HELPS; E0 PASS at stack-nc

The two amended configurations on the E0 rungs, 24 seeds
[measured]:

| config | R2/W2 accept | C2/DK accept | viol R2/C2 |
|---|---|---|---|
| plain (inherited) | 19.615 | 24.615 | 0.590 / 0.512 |
| shipped (κ.25, κ_c.1) | 16.0 | 6.769 | 0.575 / 0.445 |
| stack-lo (κ.05, κ_c.1) | 12.154 | 10.154 | 0.599 / 0.483 |
| **stack-nc (κ.25, κ_c 0)** | **25.846** | **23.385** | 0.585 / 0.501 |

The ablation separates the components exactly: with commitment OFF
and the full itch, R2 acceptance rises ABOVE the plain drive (+32%)
and C2 returns to plain's level — **Bar E0 PASS at stack-nc**,
lines and violation clauses cleared. With commitment ON, even a
whispered itch (κ .05) stays below plain on both rungs: incumbency,
not the itch, was the switch tax [measured by ablation]. The 0101
lesson runs backward in sequence worlds: what the dig needed —
dying with its intention — the token switch punishes; a boundary-
releasing hold is the shape a sequence-commitment would need
[mechanism-argument, named for design 0020]. Per the declared
ladder, stack-nc carries E1 (R4/W2) and E2 (R8/W2, C4/DK).
