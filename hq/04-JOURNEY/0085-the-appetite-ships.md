# Episode 0085 — The appetite ships: promoted on timing, closure exact twice (2026-08-10)

The owner's directive closed the arc's loop: "promote the coupling
with a timing-primary bar." The coupling-promotion topic froze the
bars before any run — closure by behavioral identity (P1), then the
timing-primary criteria the instrument had earned (P2 on the C1
body, P3 on the sample field) — and feature 042 shipped through the
spec-kit lifecycle as **v1.4.0**: `deficit_index` / `deficit_kappa`
on `CompletionItchPolicy` and `RecipePolicy`, keyword-only, off by
default, the effective label weight
`label_beta + κ_d·clip(1 − obs[deficit], 0, 1)` at both label read
sites. Off is bit-exact v1.3.0, proven with RNG-state equality; no
snapshot change; validation fails loudly; the surface grows
additively (inventory, Doc 0008 release notes, Doc 0005 §4.5 [V],
Doc 0010).

The closure is the episode's spine [measured]: the shipped gate,
substituted for the six-line instrument inside the *unchanged*
archived runners, reproduced **all 48 rows across both bodies with
zero field-level mismatches** — 24 rows of episode 0083's coupled
arm on the Minecraft body, 24 rows of episode 0084's coupled arm on
the sample field. The promotion bars then read off the identical
rows: crisis-diet ratio 2.22× (bar 2×), survival gap 4 (bar 4),
sated rotation 22/24, chains 24/24 on C1; hungry-bin margin +0.216
(bar +0.15) and 0 vs 16 deaths on the second body. Third exact
closure of the arc (040, 041, 042).

For the record, plainly: episode 0083's aggregate-share bar remains
**failed** — this promotion is licensed by the timing-primary
criteria the owner directed, with the mis-targeting of the
aggregate summary documented in Doc 0010, not erased by it.

What it means: the brain that ships can now be given a body whose
depletion it *feels* — and its wanting sharpens exactly when the
meter sags, on any embodiment, with one constructor line. The
measured operating point of record: κ_d = 0.1 against a [0, 1]
meter.

Reversal condition: none — records a completed build and its exact
closure. The dial's crisis behavior is pinned by the archived rows;
a regression against them is the tripwire any future change to the
label seam must clear.

Trail: specs/042-deficit-gating/ (spec → plan → tasks);
`src/pra/action/policy.py` + `recipe.py`; the coupling-promotion
topic (folder retired; registration 12bcf71, verdict in this
change-set); closure runner `closure_042.py` + row files in the
session scratchpad; Docs 0005/0008/0010; tag v1.4.0.
