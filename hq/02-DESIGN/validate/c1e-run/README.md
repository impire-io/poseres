# c1e archive — the real life, two arms (closed 2026-08-11)

Run 4's runner (`c1e_runner.py`, both arms via argv), wrapper, and the
per-segment status rows for both arms. Runs 1–3 and attempts 1–2 (their
rows, logs, snapshots, bridge logs, and post-mortem probes) live in the
2026-08-11 session scratchpad archives `c1e-attempt1/ c1e-attempt2/
c1e-run1/ c1e-run2/ c1e-run3/`; the run plan
(`../C1E-RUN-PLAN.md`) carries every amendment with its raw numbers.

Final rows: arm B (one-shot, wean 9,000) died at life-tick 14,398 — the
pantry death; arm A (respawn) manually stopped at 140,617 steps, 10
chains, 28 deaths, no learning-over-lives trend. Neither arm met the
frozen decision rule; the post-mortems returned to design, and the
design conversation returned the verdict that the scenario itself — a
synthetic metabolism in a world with a native one — was the tangent
(episode 0096).
