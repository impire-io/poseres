# Episode 0062 — The docs site: the record, rendered (2026-07-27)

Phase D's second item (feature 036, built in a parallel worktree the
day after v1.0): a static documentation site deployable by GitHub
Pages — index with the roadmap's own honest thesis, a getting-started
page verified against the real CLI, the design docs 0001–0008, a
public-API page that links Doc 0008 rather than restating it, and a
nine-entry worlds gallery (reference, the five ladder worlds, the
rover, the Gymnasium adapter, the Minecraft body), every config
snippet executed against `Config` validation before it was written
down [measured] and every episode citation read from the journey
index, recorded FAILs included (the L3 channel-static collapse, the
Minecraft emergence floor) — the site states what was measured, never
more (constitution II/IV).

The design docs are not duplicated: the Pages workflow copies
`hq/02-DESIGN/0*.md`, `GETTING-STARTED.md`, and the explainer into the
site at build time, so the repo keeps one source of truth. A unit test
guards the site against rot (every referenced file exists, nav
resolves, the workflow's copy list is glob-checked, with an in-test
negative control) [measured]. Gate on the feature: 673 passed, zero
skips — the eight new tests on top of the frozen 665
[measured].

Deployment is the one externally-gated step, and it stayed external:
the automated landing was not permitted to enable Pages (publishing is
the owner's act), so the exit's deploy clause is **pending the owner**
— one click (Settings → Pages → Source: GitHub Actions) or
`gh api -X POST repos/impire-io/poseres/pages -f build_type=workflow`,
then the workflow deploys on the next docs push or manual dispatch.
Until the first real Jekyll render the plugin-set assumption is
stated, not proven [judgment].

Reversal condition: none — records a completed build; the roadmap exit
("docs deployed, linked from README") is read at landing and the site
is regenerated by every docs-touching push thereafter.

Trail: `specs/036-docs-site/`; `docs/`; `.github/workflows/docs.yml`;
`tests/unit/test_docs_site.py`; site
https://impire-io.github.io/poseres/.
