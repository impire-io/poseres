# Episode 0104 — The body that lives is the default: feature 044 / v2.2.0 (2026-08-16)

The owner's call, on the strength of episode 0103's replicated
self-feeding lives: the survival body promotes into the default
anatomy. `c1_anatomy()` with no arguments now returns design 0015's
measured operating point — the property body plus the mouth
(`use_held`, edible affordance), the distal senses (drops + glance),
the flood channel, and the palate's worth channel: obs_dim 86,
n_actions 13 — and the bridge wire defaults in lockstep (SURVIVAL
default-on, FLOOD=intrusion and AIM=worth when unset), with
`contract_check` inferring all three from the hello table.

The mechanism is sentinel defaults, and the discipline is the
feature-033 precedent (the last time the default body changed):
**only the zero-override call changed.** `survival=False` /
`SURVIVAL=0` is the pre-044 property body (32/12);
`crafting=False` remains the exact feature-027 body (14/8, no new
error); `survival=True` alone remains obs 73 and `survival=True,
flood=True` remains 77 — pinned by the default-matrix test
[measured]. `C1_MINING_INDEX`/`C1_POCKET_TOTAL_INDEX` are unchanged
(14/15, derived before the widened hand), so no shipped consumer of
the anatomy constants moves. The frozen T1–T6 suite runs
non-Minecraft worlds and is untouched; the full gate ran green.

Reversal condition: none — records a completed promotion of a
measured operating point; the operating point's own reversal watch
lives in episode 0103 (a blessed-stack cohort failing to sustain
reopens the stack, not the default mechanics).

Trail: `specs/044-survival-default/`; design
[0015](../02-DESIGN/0015-native-survival.md); episode
[0103](0103-native-survival.md); specs/027 contract PROMOTED note;
Doc 0008 v2.2.0 note; tag v2.2.0.
